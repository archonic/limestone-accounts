class Placeholder < ActiveRecord::Migration[5.2]
  def change
    # ... *tumbleweed*...

    # This file is in place only to satisfy rake migration status.
    # Migrations are for sharing changes a database over time.
    # Since this is a boilerplate, we only share the creation snapshot, which is schema.rb
    # Use rake:db:setup to create and seed a new env.
  end
end
